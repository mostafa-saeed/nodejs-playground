{
  "name": "tcp-tunnel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "invoke": "ts-node src/command",
    "start:server": "ts-node src/server",
    "start:tunnel": "ts-node src/tunnel",
    "start:tunnel-server": "ts-node src/tunnel-server",
    "dev:server": "nodemon --exec ts-node src/server",
    "dev:tunnel-server": "nodemon --exec ts-node src/tunnel-server",
    "dev:tunnel": "nodemon --exec ts-node src/tunnel",
    "dev:invoke": "nodemon --exec ts-node src/command",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.0.0",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2"
  }
}
