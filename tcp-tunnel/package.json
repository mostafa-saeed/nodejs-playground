{
  "name": "tcp-tunnel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:server": "node src/server",
    "start:tunnel": "node src/tunnel",
    "dev:server": "nodemon --exec ts-node src/server",
    "dev:tunnel-server": "nodemon --exec ts-node src/tunnel-server",
    "dev:tunnel": "nodemon --exec ts-node src/tunnel",
    "invoke": "nodemon --exec ts-node src/command",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.0.0",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2"
  }
}
